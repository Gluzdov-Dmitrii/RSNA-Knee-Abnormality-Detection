# Daily report: Day 3 / 2026-09-07

## Before

- UTC timestamp: 2026-09-07T03:08:17Z
- Asia/Novosibirsk timestamp: 2026-09-07T10:08:17+07:00
- Remaining submissions / reset time: 5 remaining today (0 used); lifetime 30. Daily cap 5.
- Available GPU quota and slots: GPU 17.04 h used / 12.96 h remaining of 30.00 h (refresh 2026-09-12T00:00:00Z). ARC `arc2-s1-program077-fork` and `duck-qwen3-8-flash-next-nvfp4-mtp` both COMPLETE. Concurrent GPU jobs: 0.
- Lifetime submissions: 30.
- Our best score / rank / team count: `0.937` / rank `183` / `3 169` teams (Day-2 snapshot). Leader `0.954`. 10th `0.950`. Gold boundary still ~rank 16 / `0.948–0.949`.
- Git HEAD: `24b981a` on `main` before this note.

## Planned batch and readiness

| ID | Hypothesis | Dependencies | Artifact state | Output filename | Ready/blocker |
|---|---|---|---|---|---|
| S11 | Pilkwang V1 head on frozen DINO + 0.75/0.25 rank blend with A0 | FOLDS_V1; DINO_CACHE_V1; LABEL_PILKWANG_V1 | labels pinned | `submission_S11.csv` | missing folds/cache/trained head |
| S12 | Three-teacher median consensus | folds + cache + three teachers | labels pinned | `submission_S12.csv` | missing folds/cache/trained head |
| S13 | Three-teacher mean consensus | same | labels pinned | `submission_S13.csv` | missing folds/cache/trained head |
| S14 | Median + disagreement mask 0.50 | same | labels pinned | `submission_S14.csv` | missing folds/cache/trained head |
| S15 | OOF winner + expert-58 `×6` | LABEL_ABLATIONS@evaluated | waiting | `submission_S15.csv` | waiting_for_label_ablation_evidence |

Day-2 S06–S10 are terminal-scored (`0.937/0.937/0.936/0.936/0.936`). They were not resent.

## Results

No Day-3 competition submits. Quota left **5/5**. Substituting a public fork for S11–S15 is forbidden: the hypothesis is a shared-feature head on frozen A0 DINO embeddings inside one internet-off unified graph, not a standalone public notebook.

Pinned teacher files (hashes only; raw CSVs stay out of git):

- Pilkwang V1 `report_labels_v2.csv` SHA-256 `6f704a7b…0920` — matches `ops/EXPERIMENT_SPECS.md`
- Steven V6 `llm_labels_v2.csv` SHA-256 `3c082a98…f144`
- Lixin V1 `labels_llm_gpt56sol.csv` SHA-256 `a79b05e6…67ae`

## Interpretation

- S08–S10 public `−0.001` vs A0 is noise; A0 remains the control graph.
- Day 3 cannot score until locked study-level folds, a frozen DINO-S cache (≤8 GPU-h), and five small heads (together ≤4 GPU-h) exist, then a unified A0+Li rehearsal `< 8.5 h`.
- Standalone Li instead of `0.75 rank(A0)+0.25 rank(head)` would be a different hypothesis and is not used to fill the quota.

## Prepared for next package

- Ready artifacts: three teacher datasets pinned and hashed
- Running preparation jobs: none
- Blockers: `FOLDS_V1` and `DINO_CACHE_V1` still missing; no trained S11–S14 heads
- Exact next S-ID: S11 after folds+cache+head; do not skip to S16+
- Quota left: 5 / 5 today; lifetime 30
