# Daily report: Day 3 / 2026-09-07

## Before

- UTC timestamp: 2026-09-07T10:22:00Z (this session recon); earlier label pin 2026-09-07T03:08:17Z
- Asia/Novosibirsk timestamp: 2026-09-07T17:22:00+07:00
- Remaining submissions / reset time: 5 remaining today (0 used); lifetime 30. Daily cap 5.
- Available GPU quota and slots: Kaggle GPU 19.84 h used / 10.16 h remaining of 30.00 h (refresh 2026-09-12T00:00:00Z). `dmitriigluzdov/duck-qwen3-8-flash-next-nvfp4-mtp` RUNNING. Biohub EXP128, ARC-2 S3 selector, and RSNA week-1 A0 kernel COMPLETE. Do not start a second RSNA GPU session while Duck holds a slot.
- Lifetime submissions: 30.
- Our best score / rank / team count: `0.937` / rank `183` / `3 169` teams (Day-2 snapshot). Leader `0.954`. Gold boundary still ~rank 16 / `0.948–0.949`.
- Git HEAD and working-tree state: `c3179b9` on `main` at session start.
- Running Kaggle kernels: Duck RUNNING; RSNA week-1 kernel COMPLETE.

## Planned batch and readiness

| ID | Hypothesis | Dependencies | Artifact state | Output filename | Ready/blocker |
|---|---|---|---|---|---|
| S11 | Pilkwang V1 head on frozen DINO + 0.75/0.25 rank blend with A0 | FOLDS_V1; DINO_CACHE_V1; LABEL_PILKWANG_V1 | folds ready | `submission_S11.csv` | missing DINO cache / trained head |
| S12 | Three-teacher median consensus | folds + cache + three teachers | folds ready | `submission_S12.csv` | missing DINO cache / trained head |
| S13 | Three-teacher mean consensus | same | folds ready | `submission_S13.csv` | missing DINO cache / trained head |
| S14 | Median + disagreement mask 0.50 | same | folds ready | `submission_S14.csv` | missing DINO cache / trained head |
| S15 | OOF winner + expert-58 `×6` | LABEL_ABLATIONS@evaluated | waiting | `submission_S15.csv` | waiting_for_label_ablation_evidence |

Day-2 S06–S10 remain terminal-scored. They were not resent. Day-3 competition submits were not made.

## Results

No Day-3 competition submits. Quota left **5/5**.

CPU progress this session: locked `FOLDS_V1`.

| Asset | Status | SHA-256 / ref |
|---|---|---|
| `LABEL_PILKWANG_V1` | ready | `6f704a7bdb2f894cc49445b19ba7c4378c3f548d3449e00361e10044bee40920` |
| `LABEL_STEVEN_V6` | ready | `3c082a987939528a55202028f655bd6f9dc5c9e28eff5058bbae7ef088a9f144` |
| `LABEL_LIXIN_V1` | ready | `a79b05e609336811d51c97f9a48f00ea8004244a1a80fe886504a62bb39867ae` |
| `FOLDS_V1` | ready | `3086df3341333f44adb883292da386857c3230eaa2d501514ddf827a2da11b1a` (`ops/assets/FOLDS_V1/folds.csv`) |
| `DINO_CACHE_V1` | missing | needs train images + GPU allocation |

`FOLDS_V1`: 4407 studies, seed 2026, 5 iterative-stratified folds (sizes 882/881/881/881/882). Pilkwang V1 missing one train UID (Steven/Lixin have it); that UID kept with an all-zero stratify row.

## Evidence

- Exact source refs and versions: official `train.csv` UIDs only; Pilkwang V1 as above. Reports were not written into git or NSU.
- Data/labels/cache/folds/model/code hashes: folds SHA above; DINO cache absent.
- Visible 3-row output hashes (not hidden hashes): n/a (no Day-3 kernel)
- OOF macro and 12 per-target AUC: n/a
- Gold-58 sanity and bootstrap interval: n/a
- OOF raw/rank correlations: n/a
- Hidden runtime estimate and peak VRAM/RAM: n/a
- Errors by language/site/scanner/protocol/plane/series availability: n/a

## NSU recon (approved local Windows, `hasee\dmitry`)

Client: `C:\WINDOWS\System32\OpenSSH\ssh.exe`.

| Alias | Identity command | Result |
|---|---|---|
| `nsu-quadro` | `hostname` | exit 0, `prepost` |
| `nsu-a100` | `hostname` | exit 0, `ngpu01` |
| `nsu-pc` | `whoami` | exit 0, `desktop-7t0uo8i\user` |

Live resources: Quadro RTX 6000 24 GB idle; both A100 80 GB idle (separate UUIDs, not one pool); RTX 3080 10 GB idle (~311 MiB). Linux home `/home/scientists/gluz_d_s` mode 700, writable. `/data` is writable and shared — not used for competition data. `sinfo` timed out (exit 124). Registered Linux Kaggle root was missing at inspect; created under the home tree with mode 700. `nsu-pc` remote profile is `C:\Users\User` (not the local HASEE profile); `%USERPROFILE%\kaggle` was created. PC disks: C ~401 GiB free, D ~12 GiB free, E ~1.67 TiB free. GPU work remains `BLOCKED_COORDINATION` until the atomic lease protocol exists; also `BLOCKED_DEPENDENCY` for DINO cache (no local DICOMs). Duck holds the Kaggle GPU slot — did not preempt. Remote `FOLDS_V1` SHA matches on both Linux hosts.

Official rules (2026-09-07): Competition Data may be used for the competition and academic research under the MIRA license; do not transmit/publish data to non-participants. External data/tools allowed if public or reasonable-cost. Private code sharing between separate teams is forbidden. NSU is the same participant’s machines; keep data in the 700 home tree, not world-readable `/data`. No 570 GB DICOM transfer this session.

## Interpretation

- S08–S10 public `−0.001` vs A0 is noise; A0 remains the control graph.
- Day 3 still cannot score until `DINO_CACHE_V1`, S11–S14 heads, S15 OOF winner, and a unified A0+Li rehearsal `< 8.5 h`.
- Standalone Li instead of `0.75 rank(A0)+0.25 rank(head)` is not used to fill quota.
- Kaggle weekly GPU remaining (~10 h) cannot host the planned cache+heads envelope if Duck and later ARC also need it. Cache belongs on NSU/Kaggle after allocation, not on a second concurrent Kaggle GPU job.

## Prepared for next package

- Ready artifacts: three teacher datasets pinned; `FOLDS_V1` locked
- Running preparation jobs: none (no GPU lease)
- Blockers: `DINO_CACHE_V1`; GPU `BLOCKED_COORDINATION` on NSU; Duck RUNNING on Kaggle; no local train DICOMs
- Exact next S-ID: S11 after cache+head; do not skip to S16+
- Quota left: 5 / 5 today; lifetime 30

## Cleanup receipt

- Deleted owned inspect scripts and temporary NSU bootstrap JSON under `tmp/` after verification.
- Official `train.csv` (contains Report text) kept gitignored under `data/official/`; not copied to NSU.
- No GPU allocation to release.
