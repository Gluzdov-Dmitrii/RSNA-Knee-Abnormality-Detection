# Пересмотр compute plan R2 — 2026-09-07

Задача: снять конкуренцию за Kaggle GPU, используя внешние RTX 6000/3080/2×A100.
Результат: `LOCAL_COMPUTE_PLAN.md`, `ops/compute_plan.json`, обновлённый executor prompt.

## Проверенные факты

- SSH из sandbox (`hasee\codexsandboxoffline`) не разрешает три aliases.
  Через стандартное разрешённое выполнение клиента пользователя (`hasee\dmitry`)
  все три identity checks успешны; VPN/SSH configuration не менялась.
- `prepost`: Quadro RTX 6000 24576 MiB, 5 MiB занято, 0% utilization; 251 GiB RAM.
- `ngpu01`: две A100 по 81920 MiB, обе 0 MiB/0% на момент проверки; 251 GiB RAM.
- `nsu-pc`: identity `desktop-7t0uo8i\user`; 3080 10 GB и 32 GB RAM взяты из
  внешнего SETUP_STATUS/README, повторный GPU load test не проводился.
- Shared NFS `/home` около 7 TiB available, `/data` около 39 TiB available;
  personal quota не подтверждена. RSNA subtree mode 700.
- Live control record от `2026-09-07T12:07:18.987476+00:00`:
  initialized=true, DIRECT_USER_AUTHORIZED, Slurm не обязателен,
  ALLOWED_WITH_RESOURCE_RESERVATION. Файл queue script установлен в `_control`.
- RSNA manifest всё ещё запрещает reports/DICOM/credentials; на remote data есть
  только FOLDS_V1. Сам manifest этой задачей не менялся.
- `kaggle quota` около 12:10 UTC: GPU used 20.90 h, remaining 9.10 h, total 30 h,
  refresh 2026-09-12T00:00:00. Submission limits: today 0, lifetime 30, left 5.

## Принятые изменения плана

- Все preparation/OOF/train на НГУ; Kaggle только selected inference/submits.
- CPU-first small heads; Quadro cache; one A100 для тяжёлых branches; вторая только
  при allocation/fairness. Никакого постоянного владения GPU проектом.
- S11–S35 сохраняются, но local screening выбирает обычно 2–3, максимум 5 submissions
  из готового пакета. Добавлен local_evaluated для evidence без public receipt.
- S11, лучший S12–S14 и S15 — первые приоритеты; model/geometry/Q позднее.
- Named output workaround удалён: Day 2 уже доказал HTTP 400; только submission.csv.
- A0 export/parity без train mount обязателен: fitted train artifacts считать снаружи.
- Единая weak-label OOF reference и отдельный expert-58 crossfit; cache provenance
  должен исключать train/validation leakage.
- R2 cap до reset: Kaggle prep 0 h; visible validation soft cap 1.5 h, review 0.5 h;
  hidden billing остаётся unknown, пока нет подходящих measurements.

## Граница выполненной работы

Выполнены read-only recon и изменения планов/инструкций внутри RSNA repository.
GPU training, bulk data transfer, environment install, queue reservations, remote
manifest update и Kaggle submissions не запускались. Старые S01–S10, folds, labels,
дневные отчёты и чужие scratch files сохранены.

Следующий исполнимый этап: M0 storage/manifest + isolated ML env/shared code + 32-study
decode pilot, затем выделенный 128-study GPU pilot и full DINO cache.
